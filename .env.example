API_KEY=replace-with-32-char-hex
# On Render, set SEED_DEMO_DATA=true so every deploy/cold start reloads 15 demo calls
# for the dashboard (ephemeral disk). Locally leave off unless you want that behavior.
# SEED_DEMO_DATA=true
